<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:padding="16dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Button
        app:iconSize="28dp"
        android:id="@+id/btnVisited"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/map_visited_legend_text"
        app:iconTint="@null"
        app:icon="@drawable/blue_marker"
        style="@style/Widget.MaterialComponents.Button.TextButton.Icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        app:iconSize="28dp"
        android:id="@+id/btnToVisit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/map_not_visited_legend_text"
        app:iconTint="@null"
        app:icon="@drawable/red_marker"
        style="@style/Widget.MaterialComponents.Button.TextButton.Icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnVisited" />

</androidx.constraintlayout.widget.ConstraintLayout>